###############################################################################
# BAY 1 — BOOKING START
# Fires 5 min before booking. Turns on highbay 1 + common lights.
# Extracts customer name and door code from calendar event for notification.
###############################################################################
- id: 'bay1_booking_start'
  alias: Bay 1 Booking - START
  description: >
    5 min before booking: turn on highbay 1 and common lights.
  triggers:
    - trigger: calendar
      event: start
      entity_id: calendar.lane_1_bowling_machine
      offset: "-0:05:00"
  conditions:
    - condition: state
      entity_id: input_boolean.bay1_blocked
      state: 'off'
  actions:
    - variables:
        door_code: >-
          {{ (trigger.calendar_event.description or '')
             | regex_findall('DOOR CODE: (\d+)') | first | default('') }}
        customer_name: >-
          {{ (trigger.calendar_event.description or '')
             | regex_findall('Customer: (.+)') | first | default('Guest') | trim }}
        booking_title: "{{ trigger.calendar_event.summary | default('Booking') }}"
    - action: input_boolean.turn_on
      target:
        entity_id: input_boolean.bay1_helper
    - action: light.turn_on
      target:
        entity_id: light.highbay_1
    - action: light.turn_on
      target:
        entity_id:
          - light.inside_wall_lights
          - light.fluro_x_4
          - light.mezzanine_wall_lights
    # Notification disabled - re-enable and set target device when ready
    # - action: notify.notify
    #   data:
    #     title: "Bay 1 - Starting in 5 min"
    #     message: >-
    #       {{ customer_name }} | {{ booking_title }}.
    #       {% if door_code != '' %}Door code: {{ door_code }}.{% endif %}
  mode: single

###############################################################################
# BAY 1 — BOOKING END
# Fires 5 min after booking ends.
# Skips entirely if a back-to-back booking has already started on Bay 1.
# Only turns off common lights if ALL 5 bays are now empty.
###############################################################################
- id: 'bay1_booking_end'
  alias: Bay 1 Booking - END
  description: >
    5 min after booking: turn off highbay 1.
    Common lights off only when all bays are empty.
  triggers:
    - trigger: calendar
      event: end
      entity_id: calendar.lane_1_bowling_machine
      offset: "0:05:00"
  actions:
    - condition: template
      value_template: "{{ is_state('calendar.lane_1_bowling_machine', 'off') }}"
    - action: input_boolean.turn_off
      target:
        entity_id: input_boolean.bay1_helper
    - action: light.turn_off
      target:
        entity_id: light.highbay_1
    - condition: template
      value_template: >-
        {{ is_state('input_boolean.bay1_helper', 'off') and
           is_state('input_boolean.bay2_helper', 'off') and
           is_state('input_boolean.bay3_helper', 'off') and
           is_state('input_boolean.bay4_helper', 'off') and
           is_state('input_boolean.bay5_helper', 'off') }}
    - action: light.turn_off
      target:
        entity_id:
          - light.inside_wall_lights
          - light.fluro_x_4
          - light.mezzanine_wall_lights
  mode: single

###############################################################################
# BAY 2 — BOOKING START
###############################################################################
- id: 'bay2_booking_start'
  alias: Bay 2 Booking - START
  description: >
    5 min before booking: turn on highbay 2 and common lights.
  triggers:
    - trigger: calendar
      event: start
      entity_id: calendar.lane_2_bowling_machine
      offset: "-0:05:00"
  conditions:
    - condition: state
      entity_id: input_boolean.bay2_blocked
      state: 'off'
  actions:
    - action: input_boolean.turn_on
      target:
        entity_id: input_boolean.bay2_helper
    - action: light.turn_on
      target:
        entity_id: light.highbay_2
    - action: light.turn_on
      target:
        entity_id:
          - light.inside_wall_lights
          - light.fluro_x_4
          - light.mezzanine_wall_lights
  mode: single

###############################################################################
# BAY 2 — BOOKING END
###############################################################################
- id: 'bay2_booking_end'
  alias: Bay 2 Booking - END
  description: >
    5 min after booking: turn off highbay 2.
    Common lights off only when all bays are empty.
  triggers:
    - trigger: calendar
      event: end
      entity_id: calendar.lane_2_bowling_machine
      offset: "0:05:00"
  actions:
    - condition: template
      value_template: "{{ is_state('calendar.lane_2_bowling_machine', 'off') }}"
    - action: input_boolean.turn_off
      target:
        entity_id: input_boolean.bay2_helper
    - action: light.turn_off
      target:
        entity_id: light.highbay_2
    - condition: template
      value_template: >-
        {{ is_state('input_boolean.bay1_helper', 'off') and
           is_state('input_boolean.bay2_helper', 'off') and
           is_state('input_boolean.bay3_helper', 'off') and
           is_state('input_boolean.bay4_helper', 'off') and
           is_state('input_boolean.bay5_helper', 'off') }}
    - action: light.turn_off
      target:
        entity_id:
          - light.inside_wall_lights
          - light.fluro_x_4
          - light.mezzanine_wall_lights
  mode: single

###############################################################################
# BAY 3 — BOOKING START
###############################################################################
- id: 'bay3_booking_start'
  alias: Bay 3 Booking - START
  description: >
    5 min before booking: turn on highbay 3 and common lights.
  triggers:
    - trigger: calendar
      event: start
      entity_id: calendar.lane_3_truman_or_bowling_machine
      offset: "-0:05:00"
  conditions:
    - condition: state
      entity_id: input_boolean.bay3_blocked
      state: 'off'
  actions:
    - action: input_boolean.turn_on
      target:
        entity_id: input_boolean.bay3_helper
    - action: light.turn_on
      target:
        entity_id: light.highbay_3
    - action: light.turn_on
      target:
        entity_id:
          - light.inside_wall_lights
          - light.fluro_x_4
          - light.mezzanine_wall_lights
  mode: single

###############################################################################
# BAY 3 — BOOKING END
###############################################################################
- id: 'bay3_booking_end'
  alias: Bay 3 Booking - END
  description: >
    5 min after booking: turn off highbay 3.
    Common lights off only when all bays are empty.
  triggers:
    - trigger: calendar
      event: end
      entity_id: calendar.lane_3_truman_or_bowling_machine
      offset: "0:05:00"
  actions:
    - condition: template
      value_template: "{{ is_state('calendar.lane_3_truman_or_bowling_machine', 'off') }}"
    - action: input_boolean.turn_off
      target:
        entity_id: input_boolean.bay3_helper
    - action: light.turn_off
      target:
        entity_id: light.highbay_3
    - condition: template
      value_template: >-
        {{ is_state('input_boolean.bay1_helper', 'off') and
           is_state('input_boolean.bay2_helper', 'off') and
           is_state('input_boolean.bay3_helper', 'off') and
           is_state('input_boolean.bay4_helper', 'off') and
           is_state('input_boolean.bay5_helper', 'off') }}
    - action: light.turn_off
      target:
        entity_id:
          - light.inside_wall_lights
          - light.fluro_x_4
          - light.mezzanine_wall_lights
  mode: single

###############################################################################
# BAY 4 — BOOKING START
###############################################################################
- id: 'bay4_booking_start'
  alias: Bay 4 Booking - START
  description: >
    5 min before booking: turn on highbay 4 and common lights.
  triggers:
    - trigger: calendar
      event: start
      entity_id: calendar.lane_4_run_up_net
      offset: "-0:05:00"
  conditions:
    - condition: state
      entity_id: input_boolean.bay4_blocked
      state: 'off'
  actions:
    - action: input_boolean.turn_on
      target:
        entity_id: input_boolean.bay4_helper
    - action: light.turn_on
      target:
        entity_id: light.highbay_4
    - action: light.turn_on
      target:
        entity_id:
          - light.inside_wall_lights
          - light.fluro_x_4
          - light.mezzanine_wall_lights
  mode: single

###############################################################################
# BAY 4 — BOOKING END
###############################################################################
- id: 'bay4_booking_end'
  alias: Bay 4 Booking - END
  description: >
    5 min after booking: turn off highbay 4.
    Common lights off only when all bays are empty.
  triggers:
    - trigger: calendar
      event: end
      entity_id: calendar.lane_4_run_up_net
      offset: "0:05:00"
  actions:
    - condition: template
      value_template: "{{ is_state('calendar.lane_4_run_up_net', 'off') }}"
    - action: input_boolean.turn_off
      target:
        entity_id: input_boolean.bay4_helper
    - action: light.turn_off
      target:
        entity_id: light.highbay_4
    - condition: template
      value_template: >-
        {{ is_state('input_boolean.bay1_helper', 'off') and
           is_state('input_boolean.bay2_helper', 'off') and
           is_state('input_boolean.bay3_helper', 'off') and
           is_state('input_boolean.bay4_helper', 'off') and
           is_state('input_boolean.bay5_helper', 'off') }}
    - action: light.turn_off
      target:
        entity_id:
          - light.inside_wall_lights
          - light.fluro_x_4
          - light.mezzanine_wall_lights
  mode: single

###############################################################################
# BAY 5 — BOOKING START
###############################################################################
- id: 'bay5_booking_start'
  alias: Bay 5 Booking - START
  description: >
    5 min before booking: turn on highbay 5 and common lights.
  triggers:
    - trigger: calendar
      event: start
      entity_id: calendar.lane_5_run_up_net
      offset: "-0:05:00"
  conditions:
    - condition: state
      entity_id: input_boolean.bay5_blocked
      state: 'off'
  actions:
    - action: input_boolean.turn_on
      target:
        entity_id: input_boolean.bay5_helper
    - action: light.turn_on
      target:
        entity_id: light.highbay_5
    - action: light.turn_on
      target:
        entity_id:
          - light.inside_wall_lights
          - light.fluro_x_4
          - light.mezzanine_wall_lights
  mode: single

###############################################################################
# BAY 5 — BOOKING END
###############################################################################
- id: 'bay5_booking_end'
  alias: Bay 5 Booking - END
  description: >
    5 min after booking: turn off highbay 5.
    Common lights off only when all bays are empty.
  triggers:
    - trigger: calendar
      event: end
      entity_id: calendar.lane_5_run_up_net
      offset: "0:05:00"
  actions:
    - condition: template
      value_template: "{{ is_state('calendar.lane_5_run_up_net', 'off') }}"
    - action: input_boolean.turn_off
      target:
        entity_id: input_boolean.bay5_helper
    - action: light.turn_off
      target:
        entity_id: light.highbay_5
    - condition: template
      value_template: >-
        {{ is_state('input_boolean.bay1_helper', 'off') and
           is_state('input_boolean.bay2_helper', 'off') and
           is_state('input_boolean.bay3_helper', 'off') and
           is_state('input_boolean.bay4_helper', 'off') and
           is_state('input_boolean.bay5_helper', 'off') }}
    - action: light.turn_off
      target:
        entity_id:
          - light.inside_wall_lights
          - light.fluro_x_4
          - light.mezzanine_wall_lights
  mode: single

###############################################################################
# BAY 1 — WEBHOOK ACCESS (manual trigger, e.g. from booking platform)
###############################################################################
- id: '1773291498207'
  alias: Booking BAY1 Access - Webhook
  description: Open Door Turn on Lights
  triggers:
  - trigger: webhook
    allowed_methods:
    - POST
    - PUT
    local_only: true
    webhook_id: -jWhm-ATigP7s_EHhGwXxgROk
  conditions:
  - condition: state
    entity_id: input_boolean.bay1_blocked
    state:
    - 'off'
  actions:
  - action: script.bay1_activate_script
    metadata: {}
    data: {}
  - action: input_boolean.turn_on
    metadata: {}
    target:
      entity_id: input_boolean.bay1_helper
    data: {}
  mode: single

###############################################################################
# SAFETY — Lights left on when nobody is there
###############################################################################
- id: '1779256315851'
  alias: Lights ON Nobody There
  description: ''
  triggers:
  - trigger: numeric_state
    entity_id:
    - sensor.main_occupancy_probability
    for:
      hours: 1
      minutes: 0
      seconds: 0
    below: 30
  conditions:
  - condition: state
    entity_id:
    - light.office_downlights
    - light.fluro_x_4
    - light.inside_wall_lights
    - light.mezzanine_wall_lights
    - light.outside_wall_lights
    - light.exhaust_fan_1
    - light.exhaust_fan_2
    - light.highbay_1
    - light.highbay_2
    - light.highbay_3
    - light.highbay_4
    - light.highbay_5
    state:
    - 'on'
    for:
      hours: 1
      minutes: 0
      seconds: 0
  actions:
  - action: script.notify_script
    metadata: {}
    data: {}
  mode: single

###############################################################################
# GIT SYNC — Nightly push to GitHub at 2am
###############################################################################
- id: 'auto_git_push'
  alias: Auto Push Config to GitHub
  description: Nightly 2am push of HA config changes to GitHub
  triggers:
    - trigger: time
      at: "02:00:00"
  actions:
    - action: shell_command.git_push
      response_variable: push_result
    - if:
        - condition: template
          value_template: "{{ push_result.returncode != 0 }}"
      then:
        - action: persistent_notification.create
          data:
            title: "GitHub Push Failed"
            message: >
              Auto-push to GitHub failed at 2am.
              Your PAT token may have expired.
              Error: {{ push_result.stderr }}
  mode: single

###############################################################################
# GIT SYNC — Pull from GitHub when repo is updated
###############################################################################
- id: 'github_deploy_webhook'
  alias: Deploy from GitHub
  description: Pulls latest config from GitHub when a push is made to the repo
  triggers:
    - trigger: webhook
      webhook_id: "7k2p9x4m3n8q1r5f"
      local_only: false
  actions:
    - action: shell_command.git_pull
    - delay: "00:00:05"
    - action: homeassistant.reload_all
  mode: single
